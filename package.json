{
  "name": "ai-studio",
  "version": "1.0.0",
  "description": "Local-first AI chat application with advanced features",
  "private": true,
  "workspaces": [
    "frontend",
    "electron"
  ],
  "scripts": {
    "install-all": "npm install && cd frontend && npm install && cd ../electron && npm install && cd ../backend && pip install -r requirements.txt",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && python main.py",
    "dev:electron": "cd electron && npm run dev",
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\" \"npm run dev:electron\"",
    "build:frontend": "cd frontend && npm run build",
    "build:electron": "cd electron && npm run build",
    "build": "npm run build:frontend && npm run build:electron",
    "package:win": "cd electron && npm run package:win",
    "package:mac": "cd electron && npm run package:mac",
    "package:linux": "cd electron && npm run package:linux",
    "package:all": "npm run build && cd electron && npm run package:all",
    "lint": "cd frontend && npm run lint",
    "test": "cd frontend && npm run test && cd ../backend && pytest"
  },
  "keywords": [
    "ai",
    "chat",
    "llm",
    "local-first",
    "electron",
    "react",
    "python",
    "fastapi",
    "rag",
    "embeddings"
  ],
  "author": "zusamstone",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
